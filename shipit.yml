dependencies:
  override: []

review:
  checklist: []

deploy:
  override:
    - ./deploy.sh:
        timeout: 1800

ci:
  allow_failures:
    - "test-code / yarn-audit"
    - "Happo (cas-administration)"
    - "Happo (cas-compliance)"
    - "Happo (cas-registration)"
    - "Happo (cas-registration1)"
    - "Happo (cas-reporting)"
    - "test-administration-e2e"
    - "test-compliance-e2e"
    - "test-dashboard-e2e"
    - "test-registration-e2e"
    - "test-registration1-e2e"
    - "test-reporting-e2e"
