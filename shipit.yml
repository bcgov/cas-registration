dependencies:
  override: []

review:
  checklist: []

deploy:
  override:
    - |
      echo "Current branch: $BRANCH"
      bash deploy.sh
      timeout: 5000

ci:
  allow_failures:
    - "yarn-audit"
