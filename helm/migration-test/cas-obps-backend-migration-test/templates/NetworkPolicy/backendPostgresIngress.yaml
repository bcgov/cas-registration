kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ include "cas-obps-backend-migration-test.fullname" . }}-backend-postgres-ingress
  labels: {{ include "cas-obps-backend-migration-test.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: cas-obps-postgres-migration-test

  ingress:
  - from:
    - podSelector:
        matchLabels:
          release: {{ include "cas-obps-backend-migration-test.fullname" . }}
          component: backend
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: {{ include "cas-obps-backend-migration-test.fullname" . }}
          component: backend
