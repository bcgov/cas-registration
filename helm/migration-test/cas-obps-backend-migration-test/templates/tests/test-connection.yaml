apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cas-obps-backend-migration-test.fullname" . }}-test-connection"
  labels:
    {{- include "cas-obps-backend-migration-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cas-obps-backend-migration-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
