# Temporary values for Monorepo-supported cas registration with cas-reporting.
# This should deploy into a namespace to trial the side-by-side deployments.

# This forces {include "cas-registration.fullname"} to use our temporary naming.
fullnameOverride: cas-bciers

backend:
  replicaCount: 2

  image:
    tag: "latest"

  route:
    host: dsh-bciers-backend-dev.apps.silver.devops.gov.bc.ca

  environment: develop

  deployRoute: true

  resources:
    limits:
      cpu: 200m
      memory: 512Mi
    requests:
      cpu: 60m
      memory: 256Mi

frontend:
  replicaCount: 2

  image:
    tag: "pr-1652"

  route:
    host: dsh-bciers-frontend-dev.apps.silver.devops.gov.bc.ca
    path: "/registration"

  environment: develop

  auth:
    keycloakAuthUrl: https://dev.loginproxy.gov.bc.ca/auth
    siteminderAuthUrl: https://logontest7.gov.bc.ca

  resources:
    limits:
      cpu: 200m
      memory: 512Mi
    requests:
      cpu: 60m
      memory: 256Mi

reportingFrontend:
  enabled: true

  image:
    tag: "pr-1652"

  route:
    host: dsh-bciers-frontend-dev.apps.silver.devops.gov.bc.ca
    path: "/reporting"

  resources:
    limits:
      cpu: 200m
      memory: 512Mi
    requests:
      cpu: 60m
      memory: 256Mi

dashboardFrontend:
  enabled: true

  image:
    tag: "pr-1652"

  route:
    host: dsh-bciers-frontend-dev.apps.silver.devops.gov.bc.ca
    path: "/"

  resources:
    limits:
      cpu: 200m
      memory: 512Mi
    requests:
      cpu: 60m
      memory: 256Mi

growthbook:
  clientKey: sdk-YMBcx9KglRDGzGTE
