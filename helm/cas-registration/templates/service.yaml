apiVersion: v1
kind: Service
metadata:
  name: {{ include "cas-registration.fullname" . }}
  labels:
    {{- include "cas-registration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cas-registration.selectorLabels" . | nindent 4 }}
