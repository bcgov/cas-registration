from enum import Enum


class RegistryTableNames(Enum):
    ACCOUNT = "account"
    ISSUANCE = "issuance"
    PROGRAM = "program"
    PROJECT = "project"
    RETIREMENT = "retirement"
    TRANSFER = "transfer"
    UNIT = "unit"
    VERIFIER = "verifier"
