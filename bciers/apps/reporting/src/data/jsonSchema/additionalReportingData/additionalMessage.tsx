import { Typography } from "@mui/material";
import { BC_GOV_BACKGROUND_COLOR_BLUE } from "@bciers/styles";
export const CapturedEmmissionsInfo = (
  <Typography variant="body2" color={BC_GOV_BACKGROUND_COLOR_BLUE}>
    Captured emissions means the emissions that otherwise would be released into
    the atmosphere, that is captured instead for further applications such as
    geological deposit and as an industrial material.
  </Typography>
);
