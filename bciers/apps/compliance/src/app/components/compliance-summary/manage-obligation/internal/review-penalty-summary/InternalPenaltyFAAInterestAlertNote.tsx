import AlertNote from "@bciers/components/form/components/AlertNote";

export const InternalPenaltyFAAInterestAlertNote = () => {
  return (
    <AlertNote>
      Financial Administration Act (FAA) interest is incurred and accrues at
      prime + 3.00% annually if the automatic administrative penalty is not paid
      on time.
    </AlertNote>
  );
};
